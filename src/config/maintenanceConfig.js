// src/config/maintenanceConfig.js
/**
 * Configuración global del estado de mantenimiento.
 * Cuando `maintenance` es `true`, la aplicación muestra la pantalla de mantenimiento.
 * Cuando `maintenance` es `false`, la aplicación funciona con total normalidad.
 */
export const maintenance = true;
